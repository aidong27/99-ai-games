# Protocol 99 Browser Fixture

This small local game exists only to test the repository's browser harness.
It is never scanned as a production Entry and is excluded from generated site
data. It exposes the real read-only contract and can be won or lost entirely
through public keyboard controls.
